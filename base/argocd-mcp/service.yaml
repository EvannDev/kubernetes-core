apiVersion: v1
kind: Service
metadata:
  name: argocd-mcp
  namespace: argocd
  labels:
    app: argocd-mcp
spec:
  selector:
    app: argocd-mcp
  ports:
  - name: http
    port: 80
    targetPort: http
