apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Aucun delta pour le lab : le ClusterSecretStore utilise le provider
# "kubernetes". Sur un vrai cluster, c'est ici qu'on patcherait le provider
# (vault / aws / gcpsm) sans toucher aux ExternalSecret des composants.
resources:
- ../../../base/external-secrets
